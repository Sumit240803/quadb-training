import React from 'react'

const Tokens = () => {
    const CONTRACT_ADDRESS = "0xYourTestnetContractAddress";
    const CONTRACT_ABI = [
      // Add only the function(s) you need
      "function mint(address to, uint256 amount) public",
    ];
    try {
        if(!win)
    } catch (error) {
        
    }
  return (
    <div>Tokens</div>
  )
}

export default Tokens